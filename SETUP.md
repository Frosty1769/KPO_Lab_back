# 🚀 Инструкция по запуску бэкенда на новом устройстве

## 📋 Требования

- Python 3.11 или выше
- Git

## 🔧 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/Frosty1769/KPO_Lab_back.git
cd KPO_Lab_back
git checkout rabochaya
```

### 2. Создание виртуального окружения

**Windows (PowerShell):**
```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
```

**Windows (CMD):**
```cmd
python -m venv venv
venv\Scripts\activate.bat
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Инициализация базы данных

```bash
python init_db.py
```

Будет создана база данных `instance/cashier.db` с пользователем по умолчанию:
- **Логин:** `admin`
- **Пароль:** `admin`

### 5. Запуск сервера

```bash
python app.py
```

Сервер запустится на:
- http://localhost:5000
- http://127.0.0.1:5000

## 📦 Установленные пакеты

- **Flask** 3.1.2 - веб-фреймворк
- **Flask-SQLAlchemy** 3.1.1 - ORM для работы с БД
- **Flask-RESTX** 1.3.2 - REST API с документацией
- **Flask-CORS** 6.0.1 - поддержка CORS
- **SQLAlchemy** 2.0.44 - библиотека для работы с БД

## 🔑 Дефолтные учетные данные

После `python init_db.py` создается администратор:
- Username: `admin`
- Password: `admin`
- Role: `admin`

## 📁 Структура проекта

```
KPO_Lab_back/
├── app.py                 # Точка входа Flask приложения
├── init_db.py            # Скрипт инициализации БД
├── requirements.txt      # Зависимости Python
├── db.py                 # Инициализация SQLAlchemy
├── engine_db.py         # Конфигурация БД
├── model/
│   └── model.py         # Модели User и Product
├── db_actions/
│   ├── user.py          # Логика работы с пользователями
│   └── products.py      # Логика работы с товарами
├── endpoints/
│   ├── user.py          # API endpoints для пользователей
│   └── products.py      # API endpoints для товаров
├── scr/
│   └── core.py          # Настройки приложения
└── instance/
    └── cashier.db       # SQLite база данных (создается автоматически)
```

## 🔗 API Документация

После запуска доступна по адресу: http://localhost:5000/

Подробная документация API находится в файле `API_DOCS.md`

## ⚙️ Настройки (scr/core.py)

- `SECRET_KEY` - секретный ключ для сессий
- `SESSION_TYPE` - тип хранения сессий (filesystem)
- `SQLALCHEMY_DATABASE_URI` - путь к БД SQLite

## 🐛 Отладка

Для просмотра DEBUG информации сервер запускается с `debug=True`. 
В терминале будут видны все запросы и информация о сессиях.

## 🛑 Остановка сервера

Нажмите `Ctrl+C` в терминале где запущен `python app.py`
